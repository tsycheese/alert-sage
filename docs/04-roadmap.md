# 开发路线图

## 1. 里程碑概览

| 版本 | 目标 | 可演示结果 |
| --- | --- | --- |
| V0 | 工程骨架 | API、Web、数据库可一键启动 |
| V1 | 核心闭环 | 告警从创建到人工确认、案例沉淀 |
| V2 | 求职展示版 | 恢复、幂等、降级、监控和 RAG 评测 |
| V3 | 扩展版 | 真实外部系统适配、告警聚合和权限 |

项目主要交付目标为 V2。

## 2. V0：工程骨架

- 创建 FastAPI、React 和 PostgreSQL 基础工程。
- 添加 Docker Compose 和 `.env.example`。
- 配置 SQLAlchemy、Alembic 和首个数据库迁移。
- 实现健康检查与前后端连通。
- 建立 pytest、前端测试和代码质量检查。

完成标准：新环境可以用一组明确命令启动，健康检查通过。

## 3. V1：完整业务闭环

### 3.1 告警管理

状态：V1.2 已完成 API、Web 页面、统一错误协议和 OpenAPI 类型生成链路。

V1.3A 已完成工作流状态契约、业务事实表、幂等约束、状态转换守卫和迁移。

V1.3B 已完成可恢复七节点 LangGraph。V1.3C 已完成异步工作流与 Web 人工确认。V1.4 已完成结构化案例、异步知识同步适配器、同步事件、查询/重试 API 与 Web 案例面板。V2.1 已完成 Dify 真实发布、检索、诊断引用和 Cloud 端到端验收。V2.2 已完成 DeepSeek 结构化诊断、模型边界防护和真实端到端验收。V2.3 已完成 Prometheus/Grafana 指标栈。V2.4 已完成结构化日志、跨异步边界关联和诊断链路时间线。V2.5 已完成事务性 Outbox、自动 Relay、退避重试和 Broker 故障恢复验收。V2.6A 已完成版本化 RAG 评测集、严格契约和确定性检索指标基础。V2.6B 已完成独立评测 Dataset、固定语料发布器、异步评测运行、逐题结果持久化、查询 API 和真实 Dify calibration/test 基线验收。V2.6C 已完成只读运行列表、详情诊断和同评测集双运行对比页面。V2.7A 已完成版本化离线演示场景、API 幂等编排、受限工具故障注入和 Git revision 注入入口。V2.7B 已完成隔离 Compose 环境中的 Playwright 核心主路径验收和失败取证。V2.7C 已完成可重复截图、架构图、能力证据映射和五分钟求职演示脚本。

- 实现告警创建、列表和详情接口。
- 使用数据库唯一约束实现幂等。
- 完成告警列表、详情和创建页面。

### 3.2 LangGraph 工作流

状态：V1.3B 已完成。

- [x] 建立工作流 State 与七个核心节点。
- [x] 实现四个模拟上下文工具，并发执行且允许部分失败。
- [x] 使用结构化模型输出生成版本化报告。
- [x] 使用 PostgreSQL Checkpointer。
- [x] 实现 `interrupt` 与批准、驳回、重新分析。

### 3.3 异步执行和页面事件

状态：V1.3C 已完成。

- [x] 引入 Celery 与 Redis。
- [x] API 投递启动/恢复任务，Worker 执行 LangGraph。
- [x] 节点事件写入 `workflow_events`。
- [x] 使用 PostgreSQL 补发与 Redis 唤醒组合的 SSE 推送页面状态。
- [x] Web 支持启动、报告展示、批准、驳回、重新分析和失败重试。

### 3.4 案例沉淀

状态：V1.4 已完成。

- [x] 批准后在同一业务事务中生成结构化案例。
- [x] 使用独立 Celery 任务和可替换适配器同步知识库。
- [x] 案例同步状态可查询、失败可重试，并保留审计事件。
- [x] Web 展示案例内容、标签、外部文档 ID 和失败重试入口。

完成标准：CPU 告警可以在 3 至 5 分钟内完整演示，且人工等待期间重启服务后仍可恢复。

## 4. V2：求职展示增强

### 4.1 可靠性

- [x] 通过可替换适配器接入 DeepSeek JSON Mode，并保留 Mock 离线模式。
- [x] 校验模型响应 Schema 与证据引用，覆盖限流重试、一次输出修复、错误脱敏和输入长度预算。
- 工具级超时、重试、熔断和部分失败降级。
- [x] 工具调用幂等键和结果复用。
- [x] 工作流失败重试及异常页面。
- [x] 使用事务性 Outbox 补强数据库与任务投递一致性。
- [x] 增加故障注入测试：Dify 超时、模型限流、Redis 重启、Worker 中断。

### 4.2 RAG

- [x] 实现 Dify Knowledge Base 发布、索引轮询、检索与来源归一化适配器。
- [x] 增加统一知识检索 API 和 Web 检索页面。
- [x] 使用 Dify Cloud 完成“批准案例 → 索引完成 → 页面检索 → 下一条诊断引用”的端到端验收。
- [x] 构建包含标准问题、相关文档、无答案标签和期望事实要点的版本化评测集。
- [x] 通过独立 Dify Dataset、Outbox/Celery 和 PostgreSQL 执行并保存 calibration/test 评测。
- 增加 pgvector 检索实现。
- 对比分段策略、TopK、混合检索和 rerank。
- [x] 输出来源命中率、Recall@K、MRR、拒答准确率、错误率和延迟报告。
- [x] 在 V2.6C Web 页面中比较运行参数、核心指标和失败问题。

### 4.3 可观测性

- [x] Prometheus 分别抓取 API 与 Celery multiprocess Worker，暴露 API、工作流、节点、工具、LLM、RAG 和案例同步指标。
- [x] Grafana 通过 provisioning 提供版本化核心 Dashboard。
- [x] 所有相关日志按可用范围包含请求、告警、运行、线程和案例关联 ID，并跨 Celery headers 传播。
- [x] 增加以 PostgreSQL 审计事件为事实来源的完整诊断链路可视化时间线。

### 4.4 测试和文档

- [x] 节点单元测试、数据库集成测试、恢复测试。
- [x] Playwright 覆盖创建告警、工具降级、等待确认、刷新恢复、批准、案例同步和幂等重放。
- [x] 添加幂等演示数据和确定性演示脚本。
- [x] 添加架构图、运行截图和五分钟演示脚本。
- 记录关键技术决策及性能数据。

## 5. V3：可选扩展

- 接入真实 Prometheus HTTP API。
- 接入 Loki/Elasticsearch 日志搜索适配器。
- 接入真实 CMDB。
- 飞书机器人及告警卡片。
- 基于 fingerprint 和时间窗口的告警聚合。
- 只读自动化工具及细粒度审批策略。
- 用户认证与 RBAC。

## 6. 推荐实现顺序

严格按以下顺序推进，避免中间件阻塞核心价值验证：

1. PostgreSQL 数据模型和告警 CRUD。
2. React 告警列表及详情。
3. 单进程 LangGraph 与模拟工具。
4. checkpoint、暂停和恢复。
5. Celery、Redis 和 SSE。
6. Dify 知识库。
7. 可靠性、监控和测试。
8. pgvector 检索及 RAG 评测。

每一步都应保留可运行状态，并通过自动化测试或明确的手工验收脚本验证。

## 7. 演示脚本目标

最终求职演示应覆盖：

1. 提交一条 CPU 告警。
2. 展示四个工具并发运行及其中一个工具降级。
3. 展示带来源证据的诊断报告。
4. 在人工确认节点停止 Worker 并重新启动。
5. 批准后从 checkpoint 恢复并生成案例。
6. 重复提交相同告警，证明幂等生效。
7. 在知识问答中检索刚生成的案例。
8. 打开 Grafana 查看节点耗时、失败率和模型调用指标。
