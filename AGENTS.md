# Alert Sage 协作开发规范

本文件约束整个仓库，目标是防止产品目标、架构边界和工程质量在持续开发中偏移。用户在当前任务中的明确要求优先；如与本文件冲突，必须指出冲突及影响。

## 1. 产品目标优先

- 需求直接跳到实现，但产品目标、用户场景或成功标准不明确时，先确认这些问题。
- 用户可能把实现方案当成真实需求时，主动区分：用户目标、当前方案、可选方案、推荐方案。
- 对低风险、可逆且不改变产品方向的局部不确定性，可以采用合理默认值继续执行，但必须说明假设。
- 不确定性会影响数据模型、权限、公共行为、成本或可逆性时，必须在实现前询问。
- 不一味迎合即时指令；存在更稳健的长期方案时，应礼貌但明确地提出。

## 2. 重大变更先说明

涉及架构、状态管理、数据模型、权限、主要依赖、路由或核心 UI 结构时，实施前简要说明：

- 目标与影响范围。
- 可能被破坏或需要兼容的模块。
- 可选方案及复杂度、扩展性、风险和成本比较。
- 推荐结论与判断依据。
- 验证方式和需要同步的文档。

目标明确、风险低、易回滚的小改动可直接实施并在结果中说明。用户已经明确的事项不重复询问。

不得为了短期跑通引入长期难维护的临时方案。确需临时处理时，标记 `TODO`，说明原因、影响、替换条件和长期方案。不得静默扩大任务范围。

## 3. 项目架构护栏

架构基线以 `/docs` 为准：

- 第一版采用模块化单体与独立 Celery Worker，不拆微服务。
- FastAPI 负责 API、校验、鉴权、SSE 和任务投递，不同步等待完整诊断流程。
- LangGraph 负责告警流程、分支、人工中断和恢复。
- Celery 负责异步调度，不作为业务状态的事实来源。
- PostgreSQL 保存业务事实和 LangGraph checkpoint。
- Redis 只保存队列、缓存、短期锁和事件广播等可重建状态。
- Dify 是可替换的知识检索服务，不编排核心告警流程。
- 模型、Dify、日志、指标和 CMDB 必须通过适配器接入，领域代码不得直接依赖供应商响应格式。
- 第一版不引入 Milvus、Kafka、Elasticsearch、Kubernetes 或多 Agent，除非需求或数据证明必要，并获得用户确认。

突破基线前，必须说明原因、替代方案、迁移成本和文档调整。

## 4. 状态与 AI 可靠性

- PostgreSQL 业务表是告警状态、决策、报告和案例的最终事实来源；LangGraph checkpoint 是执行恢复位置的事实来源。
- Redis、Celery Result Backend 和进程内存不得保存不可丢失的唯一业务状态。
- 告警、人工决策、工具调用和案例同步必须具备幂等键或数据库约束。
- LangGraph `interrupt` 所在节点必须假设恢复时会重新进入；中断前的副作用必须幂等。
- 数据模型变更必须通过迁移完成；破坏性迁移前说明兼容、回滚和数据风险，并获得确认。
- LLM 输出视为不可信输入，进入业务逻辑或数据库前必须通过结构化 Schema 校验。
- 权限、状态流转、阈值计算和幂等判断不得仅依赖 LLM 自由文本。
- 工具调用应定义超时、重试、降级和幂等策略；失败时不得伪造结果或来源。
- 有副作用或不可逆的工具默认需要人工确认；第一版不自动执行重启、扩缩容、发布或服务器命令。
- RAG 回答保留来源；检索策略变更使用固定评测集比较，不以个别示例判断。

## 5. 文档与验证

- 功能、信息架构、数据结构、权限或主要布局发生重大调整时，询问是否同步更新 `/docs`，并提出更新大纲；用户已要求同步时直接更新。
- 实现偏离文档时，说明偏离点、原因和影响，并让用户选择修改代码、更新文档或记录偏差。
- 不得把尚未实现的规划描述成已完成功能。重大架构决策应记录 ADR。
- 每次实现进行与风险匹配的验证；未运行时不得声称通过。
- 缺陷修复优先添加复现测试；跨层改动增加集成或端到端验证。
- LangGraph 变更覆盖正常路径、中断恢复、工具部分失败和重复执行；API 变更验证 Schema、错误路径和幂等；UI 变更验证加载、空数据和错误状态。
- 无法运行验证时，明确未验证内容、原因和剩余风险。不得删除测试、弱化断言或忽略类型错误来制造通过。

## 6. 安全、依赖与 Git

- 密钥只通过环境变量或密钥系统注入；仓库只提交脱敏的 `.env.example`。
- 外部告警、日志、文档、用户输入和 LLM 输出均视为不可信数据；日志默认脱敏。
- 新增主要依赖前说明用途、替代方案、维护及供应链风险和移除成本；依赖版本必须可复现。
- 未经用户明确要求，不提交、不推送、不创建分支或 Pull Request。
- 提交只包含单一目的，不混入无关格式化或用户已有修改；提交前检查范围、敏感信息、验证结果和文档一致性。
- 不覆盖用户已有提交或未提交修改，不使用破坏性 Git 命令，除非用户明确授权。

## 7. 规则分层

- `AGENTS.md`：持续生效、简短、项目级的目标与工程边界。
- `/docs`：架构细节、数据模型、测试矩阵、决策背景和操作说明。
- Skill：按任务触发的可复用复杂流程，可包含专项检查表、参考资料、脚本和产物模板。
- Hook/CI：必须机械执行、不能仅依赖 AI 自觉遵守的检查。

当某类规则需要多步骤执行、只在特定任务触发、会在多个项目复用，或需要配套脚本时，优先设计为 Skill；始终生效的底线仍保留在本文件。

## 8. 交付说明

重要实现开始前说明目标、影响、方案、风险、验证和文档计划。完成后报告实际结果、关键判断、验证证据、与设计的偏差及遗留事项，让用户理解“为什么这样做”，而不只是获得代码。
